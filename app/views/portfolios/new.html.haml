%h1 Create a new portfolio

= error_messages_for @portfolio

= form_for(@portfolio, :action => resource(@funder, :portfolios) ) do
  = partial :fields
  %p
    = submit "Create"

= link_to 'Back', url(@funder)