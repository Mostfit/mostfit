.graph_page  
  %h1
    Client added by date
  =chart(url(:graph_data, :action => 'dashboard', :id => 'client_growth',  :scope_unit => 'months', :scope_size => 3))
.graph_page
  %h1
    Per branch growth
  =chart(url(:graph_data, :action => 'dashboard', :id => 'branch_pie',  :scope_unit => 'months', :scope_size => 3))
.graph_page
  %h1
    Client cummulative growth
  = chart(url(:graph_data, :action => 'dashboard', :id => 'client_cumulative',  :scope_unit => 'months', :scope_size => 3))
.graph_page
  %h1
    Aggregate loan graph
  = chart(url(:graph_data, :action => 'total', :scope_unit => 'months', :scope_size => 3))


