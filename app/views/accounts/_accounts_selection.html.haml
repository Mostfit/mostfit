%option
- @accounts.each do |account|
  - if account.parent_id == nil
    %option{:value => account.id, :style => "font-weight: bold"}
      = account.name
  - else
    %option{:value => account.id}
      = account.name