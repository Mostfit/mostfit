%h1
  Centers for
  = @staff_member.name.capitalize

%table
  %thead
    %th
      Name
    %th
      Clients
    %th
      Loans
    %th
      Disbursed
    %th
      Repaid
    %th
      Amount In Default
  - for center in @centers
    %tr
      %td
        = link_to center.name, url(:branch_center, center.branch.id, center.id)
      %td
        = center.clients.count
      %td
        = center.clients.loans.count
      %td
        = center.clients.loans.sum(:amount)
      %td
        = center.clients.loans.payments.sum(:principal)
      %td
        = LoanHistory.all(:current => true, 'loan.client.center_id' => center.id).sum(:amount_in_default) || 0