
%h2
  Meeting day changes
%table.report.nojs{:style => "width: 40%"}
  %tr.header
    %th
    %th
      Valid from
    %th
      Valid upto
    %th
      Meeting day
    %th
      
  - @meeting_days.each do |day|
    %tr
      %td
      %td
        = day.valid_from
      %td
        = day.valid_upto
      %td
        = day.meeting_day_string
      %td
        = link_to("[x]", url(:controller => "application", :action => "delete", :id => day.id, :model => "center_meeting_days", :return => "/branches/#{@center.branch.id}/centers/#{@center.id}"))