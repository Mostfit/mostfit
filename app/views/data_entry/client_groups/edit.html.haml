%h1
  ClientGroups controller, edit action
%p
  Edit this file in
  %tt
    app/views/client_groups/edit.html.erb
= error_messages_for @client_group

= form_for(@client_group, :action => url(:client_group, @client_group)) do
  %p
    = submit "Update"

= link_to 'Show', url(:client_group, @client_group)
|
= link_to 'Back', url(:client_groups)