%h1
  -if @center
    == Edit the '#{@center.name}' center details
  -else
    Choose center to edit

= error_messages_for @center
- if not @center
  %p
    Please select a center that you want to edit
  %table.form
    = form_for(@center, :action => url(:enter_centers, :action => 'edit')) do
      %tr
        %td &nbsp;
      %tr
        %td &nbsp;
        %td
          Center ID or Name:
        %td
          = text_field :id, :size => 8
        %td
          = submit "Proceed..."
- elsif @center
  = form_for(@center, :action => url(:enter_centers, :action => 'update', :id => @center.id)) do
    = partial :fields, :submit_text => "Save changes"
