%h1 Register a payment

= error_messages_for @payment

= form_for(@payment, :action => resource(@branch, @center, @client, @loan, :payments)) do
  = partial :fields

  = submit "Register payment"

= link_to 'Back', resource(@branch, @center, @client, @loan, :payments)