%h1
  Area
  = @area.name
= error_messages_for @area

= form_for(@area, :action => url(:area, @area)) do
  = partial :fields
  = submit "Update #{@area.name.capitalize}"
  &nbsp;&nbsp;&nbsp;
  %b
    Or
  &nbsp;&nbsp;&nbsp;
  = link_to 'Show', url(:areas), :class => "grey_button"
  &nbsp;&nbsp;&nbsp;
  |
  &nbsp;&nbsp;&nbsp;
  = link_to 'Back', url(:area, @area), :class => "grey_button"