%h1 ClientGroups controller, new action

%p 
  Edit this file in 
  %tt app/views/client_groups/new.html.erb

= error_messages_for @client_group

= form_for(@client_group, :action => url(:client_groups) ) do
  %p
    = submit "Create"

= link_to 'Back', url(:client_groups)