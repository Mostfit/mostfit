%xml
  - for client_group in @client_groups
    %group
      %id #{client_group.id}
      %name #{client_group.name}
      %center_id #{client_group.center.id}
      %center_name #{client_group.center.name}
      

